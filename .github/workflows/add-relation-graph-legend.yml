name: Add relation graph legend
on:
  push:
    paths:
      - '.github/scripts/add_relation_graph_legend.py'
      - '.github/workflows/add-relation-graph-legend.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python .github/scripts/add_relation_graph_legend.py
      - name: Commit
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.html
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "Add relation color legend to graph"
          git push
